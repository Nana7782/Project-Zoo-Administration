const animalSelect = document.getElementById(
  "animal-select"
) as HTMLSelectElement;
const animalName = document.getElementById("name-input") as HTMLInputElement;
const animalBirthdate = document.getElementById(
  "year-input"
) as HTMLInputElement;
const animalContinentSelect = document.getElementById(
  "continent-select"
) as HTMLSelectElement;
const animalNeedsSelect = document.getElementById(
  "needs-select"
) as HTMLInputElement;
const animalEnclosureSelect = document.getElementById(
  "enclosure-select"
) as HTMLSelectElement;
const submitAnimalButton = document.getElementById(
  "submit-animal"
) as HTMLButtonElement;
